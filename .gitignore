# dependencies / build
node_modules/
.next/
tsconfig.tsbuildinfo
next-env.d.ts

# secrets & credentials — never commit
.env
.env.*
!.env.template
!worker/.env.example

# client-specific config (public repo) — keep the example, not the real brief
config/business_context.json

# runtime data / local scratch — never commit
data/

# local agent scratch / misc
.impeccable/
*.log
.DS_Store

# app secrets (SSH keys, tokens)
secrets/
